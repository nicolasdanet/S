
// -----------------------------------------------------------------------------------------------------------
// -----------------------------------------------------------------------------------------------------------

TTT_BEGIN (TimeSeed, "Time - Seed")

    t_seed seed1, seed2;
    
    int i;
    int failed = 0;

    for (i = 0; i < TEST_LOOP; i++) {
    //
    seed1 = time_makeRandomSeed();
    seed2 = time_makeRandomSeed();

    failed += (seed1 == seed2);     /* Expect consecutive seeds NOT equals. */
    //
    }

    TTT_EXPECT (failed == 0);

TTT_END

// -----------------------------------------------------------------------------------------------------------
// -----------------------------------------------------------------------------------------------------------
